const chromeOptions = {
	headless: false,
	// headless: true,
	// defaultViewport: null,
	// args: ["--incognito", "--no-sandbox", "--single-process", "--no-zygote"],
};

module.exports = chromeOptions;
